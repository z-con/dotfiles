# Screensaver and lock-on-idle are now handled natively by the Omarchy shell
# (~/.config/omarchy/shell.json "idle" settings). This file only owns the
# lock-before-sleep/wake hooks and the suspend-after-idle timeout below,
# neither of which the shell covers.

general {
    lock_cmd = omarchy-system-lock                         # lock screen and 1password
    before_sleep_cmd = OMARCHY_LOCK_ONLY=true omarchy-system-lock    # lock before suspend without scheduling display off.
    after_sleep_cmd = sleep 1 && omarchy-system-wake                 # delay for PAM readiness, then turn on display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

# Suspend after 15 minutes of idle (will auto-hibernate after 1h via systemd)
listener {
    timeout = 900
    on-timeout = systemctl suspend-then-hibernate
}
